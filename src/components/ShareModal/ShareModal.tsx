import { Dispatch, SetStateAction } from "react"

const ShareModal = ({modalOpened, setModalOpened}: {modalOpened: boolean, setModalOpened: Dispatch<SetStateAction<boolean>>}) => {
  return (
    <div>ShareModal</div>
  )
}

export default ShareModal
